version: "3.8"
services:
  db:
    image: "postgres:13"
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=coach
      - POSTGRES_PASSWORD=coach
      - POSTGRES_DB=coach
